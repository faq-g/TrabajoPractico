//descargar
git init
//iniciar
git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
git config --global -e (chekear)

git add . (Va a pasar todos los archivos al area de ensayo)

con git status (veo el estado los archivos)

git commit -m "first commit" (crea una copia con el nombre de la descripcion y
pasa los archivos al repositorio local)
git branch -M main (crea la rama main)
git remote add origin https://github.com/faq-g/TrabajoPractico.git (relacione mi git con github)
git push -u origin main (subir los archivos a github)


cuando hago cambios en el proyecto y quiero hacer otra copia

git add . (Va a pasar todos los archivos al area de ensayo)
git commit -m "descripcion" (Fecha, version)
git push (para subirlo a GitHub)